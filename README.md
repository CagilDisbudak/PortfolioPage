# Personal Portfolio

A one-page portfolio built with React + Vite + TypeScript + TailwindCSS + Framer Motion.

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm preview
```

NPM equivalents:

```bash
npm install
npm run dev
npm run build
npm run preview
```
